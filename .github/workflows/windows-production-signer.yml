name: Windows production signer foundation

on:
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/windows-cng-key-provider-runtime/**"
      - "crates/windows-production-signer-runtime/**"
      - "docs/architecture/41-tpm-cng-production-signer.md"
      - ".github/workflows/windows-production-signer.yml"
  push:
    branches:
      - main
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/windows-cng-key-provider-runtime/**"
      - "crates/windows-production-signer-runtime/**"
      - "docs/architecture/41-tpm-cng-production-signer.md"
      - ".github/workflows/windows-production-signer.yml"

permissions:
  contents: write

jobs:
  policy:
    name: Canonicalize and verify production signer contracts
    runs-on: ubuntu-24.04
    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Materialize canonical formatting and lockfile
        run: |
          cargo fmt --all
          cargo generate-lockfile

      - name: Commit canonical source when needed
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          if git diff --quiet -- Cargo.lock crates/windows-production-signer-runtime crates/windows-cng-key-provider-runtime; then
            exit 0
          fi
          git config user.name "ergaxiom-ci"
          git config user.email "ergaxiom-ci@users.noreply.github.com"
          git add Cargo.lock crates/windows-production-signer-runtime crates/windows-cng-key-provider-runtime
          git commit -m "Canonicalize Windows CNG provider"
          git push origin "HEAD:${{ github.head_ref }}"

      - name: Check workspace formatting
        run: cargo fmt --all -- --check

      - name: Lint platform-neutral production signer boundaries
        run: |
          cargo clippy \
            -p ergaxiom-windows-production-signer-runtime \
            -p ergaxiom-windows-cng-key-provider-runtime \
            --all-targets --all-features -- -D warnings

      - name: Run platform-neutral production signer attacks
        run: |
          cargo test \
            -p ergaxiom-windows-production-signer-runtime \
            -p ergaxiom-windows-cng-key-provider-runtime \
            --all-features

  windows-cng:
    name: Compile CNG provider and enforce hardware-only probe
    runs-on: windows-2025
    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Check workspace formatting
        run: cargo fmt --all -- --check

      - name: Lint Windows CNG provider
        shell: pwsh
        run: |
          cargo clippy `
            -p ergaxiom-windows-production-signer-runtime `
            -p ergaxiom-windows-cng-key-provider-runtime `
            --all-targets --all-features -- -D warnings

      - name: Run Windows CNG provider contract tests
        shell: pwsh
        run: |
          cargo test `
            -p ergaxiom-windows-production-signer-runtime `
            -p ergaxiom-windows-cng-key-provider-runtime `
            --all-features
